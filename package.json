{
  "name": "alinhafood-desktop",
  "version": "0.1.1",
  "private": true,
  "description": "Alinhafood Desktop — operação de salão com contingência offline",
  "main": "dist/main.js",
  "author": "Alinhafood",
  "scripts": {
    "build:app": "node scripts/build-app-server.mjs",
    "build:main": "node scripts/build-main.mjs",
    "typecheck": "tsc --noEmit",
    "dev": "npm run build:main && electron .",
    "dist": "npm run build:main && electron-builder --win nsis --x64",
    "dist:dir": "npm run build:main && electron-builder --dir"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "electron-updater": "^6.3.9",
    "undici": "^7.2.0"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.7.2",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20",
    "electron": "^34.0.0",
    "electron-builder": "^25.1.8",
    "esbuild": "^0.24.2",
    "png-to-ico": "^3.0.1",
    "sharp": "^0.35.3",
    "typescript": "^5.7.2"
  }
}
